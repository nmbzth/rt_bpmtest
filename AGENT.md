# AGENT.md — 实时 BPM 检测器项目说明

## 项目概述

这是一个 **实时读取系统正在播放声音并检测 BPM** 的 Windows 桌面工具。

通过系统 loopback 直接采集当前播放声音，实时分析并显示 BPM，不需要导入音乐文件。

当前实现：

- Tkinter 图形界面
- 系统输出设备选择
- 采样率 / FFT / Hop 可调
- 7 种可选 BPM 检测方案
- 每种方案有独立的动态设置界面
- 支持每套方案设置最小/最大 BPM
- 已打包为 Windows exe

---

## 当前状态

| 项目 | 状态 |
|---|---|
| 核心音频采集 | ✅ 已完成 |
| Tkinter UI | ✅ 已完成 |
| 7 种检测方案 | ✅ 已完成 |
| 动态设置面板 | ✅ 已完成 |
| 合成节拍器 60–200 BPM 精度测试 | ✅ 已通过，最大误差 < 0.15 BPM |
| 极端 BPM 30–60 / 200–400 精度测试 | ✅ 已通过，最大误差 < 0.4 BPM |
| 真实音乐测试 | ✅ 已建立测试曲库并完成首轮测试 |
| loopback 采集修复 | ✅ 已修复并重新打包 |
| PyInstaller 打包 | ✅ 已完成 |
| v3.0 三位小数 BPM 精度 | ✅ 已实现：合成 60s 误差 ≤0.0015，σ≤0.0005 |
| 打包后 exe | ✅ `dist/RTBPM-v3.0.exe`（已重建） |

---

## 精度测试结果

### 合成节拍器

60–200 BPM 每 5 BPM 测试，7 种方案最大绝对误差：

| 方案 | 最大误差 |
|---|---|
| 流式节拍跟踪 | 0.089 |
| 两级快速/稳定 | 0.089 |
| 自适应滑动窗口 | 0.093 |
| 重叠式更新 | 0.089 |
| 自适应平滑 | 0.089 |
| 候选 BPM 跟踪 | 0.100 |
| BPM 范围加速 | 0.089 |

### 极端 BPM（30–60 / 200–400）

| 方案 | 最大误差 |
|---|---|
| 流式节拍跟踪 | 0.319 |
| 两级快速/稳定 | 0.319 |
| 自适应滑动窗口 | 0.369 |
| 重叠式更新 | 0.319 |
| 自适应平滑 | 0.319 |
| 候选 BPM 跟踪 | 0.300 |
| BPM 范围加速 | 0.319 |

详细报告：`TEST_REPORT.md`

### v3.0 三位小数精度（合成稳态）

- 实现：粗估计 + 原始音频子采样 onset 定位 + 加权最小二乘周期回归；
- 合成 321.321 / 317.777 / 320.000 / 180.000 / 148.000 / 65.000，60s 全策略：
  **最大绝对误差 ≤0.0004 BPM，σ≤0.00016 BPM**（判据 ≤0.0015 / ≤0.0005）；
- 15s / 30s / 60s σ 收敛曲线见 `TEST_REPORT.md` 与 `test_precision.py`。

### 真实音乐

已下载到 `MUSIC/` 的 10 首不同风格音乐：

- Rock
- Classical / Orchestral × 4
- Funk
- Jazz / Jazz-hop × 2
- Electronic / Corporate / Afrobeat × 2

BPM 来源已记录在 `MUSIC/README.md`。

在设置合理 BPM 范围后，大部分真实音乐误差可控制在 ±0.5 BPM 左右。  
部分古典/管弦乐曲目的节拍脉冲与曲库标注 BPM 存在半速/倍速歧义，这是行业普遍现象。

---

## 技术栈

- Python 3.10+ / 3.12
- Tkinter
- `soundcard`（loopback 采集）
- `numpy`（信号处理）
- PyInstaller（打包）

---

## 文件结构

```text
rt-bpmtest/
├── AGENT.md
├── README.md
├── TEST_REPORT.md
├── rt_bpm_app.py
├── bpm_strategies.py
├── bench_compare.py
├── rt_bpm.py
├── requirements.txt
├── build_exe.bat
├── rt_bpm.spec
├── MUSIC/
│   ├── README.md
│   └── *.mp3
└── dist/
    └── RTBPM.exe
```

---

## 核心改进

1. **内部高精度 hop**  
   即使 UI 选择 512 hop，内部分析也会使用不超过 128 的 hop，保证 BPM 分辨率。

2. **自相关峰值抛物线插值**  
   用小数 lag 代替整数 lag，消除跳变和量化误差。

3. **短周期优先选峰**  
   从高 BPM 方向寻找第一个足够强的局部峰值，减少半速/倍速误判。

4. **Onset 时间抛物线插值**  
   流式方案的 onset 时间不再停留在整数 hop 上，提升间隔计时精度。

5. **流式方案融合自相关**  
   IOI 负责快速响应，稳定后使用自相关保证精度。

6. **所有方案支持 BPM 范围**  
   用户限制搜索范围后，真实音乐准确率显著提高。

7. **避免 UI 卡顿**  
   停止/切换方案时不在 UI 线程 join 音频线程，改为独立停止事件异步退出。

8. **小窗口适配**  
   设置区改为可滚动容器，音频参数分两行显示，小窗口也能完整操作。

9. **BPM 范围数字输入**  
   最小/最大 BPM 滑块右侧增加实时数字输入框，无需确认。

10. **重新监听即清零**  
   每次开始监听时立即清空上一次 BPM、状态和候选数据，避免污染。

11. **FFT / Hop 说明**  
   UI 增加“说明”按钮，README 也补充了 FFT 与 Hop 的作用说明。

12. **输入电平条**  
   实时显示当前设备输入音量，电平越大条越满。

13. **防卡死看门狗**  
   采集超过 8 秒无数据会自动停止并提示，避免任务卡住。

---

## v2.0 性能与精度优化（已完成）

### 基线数据（优化前实测，2026-08）

| 指标 | 实测值 |
|---|---|
| 8s 窗口 flux onset 包络整窗重算 | 27.2 ms/次 |
| 单次 update 耗时（15s 缓冲已填满） | 21.5 – 48.9 ms（视策略） |
| 30s 真实音乐流式运行：队列消息数 | 323 条（≈每块音频一条，重复刷 UI） |
| 首个 BPM 结果所需音频 | 0.09 – 0.46 s |

### 问题定位

1. **每 0.5s 一次 update 都整窗重算频谱通量**（约 1370 帧 FFT），占每次
   update 80% 以上 CPU；期间采集线程停止读 loopback，形成约 30ms 的
   音频采样间隙，长期运行会周期性丢拍 → 实时精度损失。
2. **队列刷屏**：`process_audio` 在未到更新间隔时返回缓存的
   `last_result`（非 None），采集线程每块都 put 进队列，UI 以
   ~10.7 Hz 重复重绘同样的内容。
3. **自相关未归一化**：短 lag 因重叠样本多而天然偏大，选峰有系统性
   偏差，放大半速/倍速误判。
4. **线性频率谱通量**对 <60Hz 轰隆声与 >10kHz 齿音不区分，真实音乐
   （尤其古典/管弦）onset 包络噪声大。
5. 环形音频缓冲每次 push 全量 memmove（15s × 22050 样本 × 4B）。

### 优化方案（v2.0）

1. **OnsetEngine 增量式包络**：维护滚动频谱通量环形缓冲；每块音频只算
   新到达的帧（FFT 批处理），计算量 O(窗口) → O(新增样本)，单块预计
   <1ms；同时消除采样间隙。
2. **环形音频缓冲**：push 不再整段 memmove。
3. **FFT 自相关 + 无偏归一化**：除以 (N-lag) 去除短 lag 偏好；
   O(N log N) 替代 O(N²)。
4. **谐波加权选峰（comb scoring）**：候选 lag 的 2/3/4/6 倍处能量计入
   得分，半速/倍速歧义更稳健。
5. **节拍连续性**：估计时参考上一稳定 BPM，对倍频一致的候选加权，
   抑制每 0.5s 的倍频跳变。
6. **加权多频段 flux（wflux）**：<60Hz 滚降、100Hz–4kHz 主频带增强、
   >10kHz 滚降；UI 新增 Onset 方法下拉（flux / wflux / rms）。
7. **只推新结果**：`process_audio` 未到更新间隔返回 None，不再刷 UI。
8. **BpmResult 增加 proc_ms**：UI 显示每次更新的实际耗时，便于验证。
9. 版本号升级 v2.0（标题 + 重新打包 exe）。

### 验证方式

- `test_sweep.py` / `test_extreme_sweep.py`：合成节拍器精度不回归
  （±0.15 / ±0.4 BPM 目标不变）。
- `test_music.py` / `test_music_ranged.py`：真实音乐精度对比。
- 新增 `bench_compare.py`：更新耗时、流式 CPU、队列条数、首结果延迟对比。

### 实测结果（v2.0，2026-08）

| 指标 | v1.x | v2.0 |
|---|---|---|
| 单次 update 耗时（15s 满缓冲） | 21.5 – 48.9 ms | **0.19 – 0.42 ms**（≈100x） |
| push 每块（2048 帧）增量计算 | 全量 memmove + 下次整窗重算 | 0.20 – 0.33 ms |
| 30s 音乐流式总 CPU | 40–80 ms | 46–48 ms（且不再含整窗重算） |
| 队列消息（30s 流式） | 323 条（每块刷 UI） | 仅真更新条数 |
| 首个 BPM 结果 | 0.09 – 0.46 s | 0.37 – 0.65 s |
| 节拍器 60–200 最大误差 | 0.089 – 0.100 | **0.099 – 0.103**（目标 ±0.15） |
| 极端 30–60 / 200–400 最大误差 | 0.300 – 0.369 | **0.300 – 0.310** |
| 真实音乐（±12 范围，8 曲） | 大部分 ±0.5 | 除已知歧义曲目外 ≤0.5（rock 180.1 ✓） |

关键点：
- 增量包络消除了 ~30ms/次的采集间隙（旧实现每秒丢约 2×30ms 音频）。
- 修复音频环回后引擎停止计算新帧的 bug（v2 开发中首次发现，曾导致
  30s 音频只用前 20.6s 的过期包络）。
- onset 方法 A/B：flux / wflux 在本曲库结果一致（权重不改变选峰），
  rms 明显更差（jazz_summer 104→114.2）；默认保持 **flux**，wflux 作选项。
- 精度说明：v2 改用无偏自相关 + 小数 lag 谐波采样选峰；真周期落在
  整数格之间时（如 190 BPM → lag 54.4），必须用**小数 lag** 采样谐波，
  否则整数倍频候选得分反超导致半速/三分速误判（已修复并有插桩验证）。

### 已知问题修复：夢世界フォークロア 148→198 误判（v2.1）

| 项目 | 内容 |
|---|---|
| 曲目 | `上海アリス幻樂団,黄昏フロンティア - 夢世界フォークロア.mp3`（148 BPM） |
| 现象 | 从头播放每次都稳定识别为 197–198 BPM，且不会自行纠正 |
| 直接原因 | 曲目前 ~8s 存在约 198 BPM（≈148×4/3 附点/切分韵律层）的强自相关峰，与 148 峰相当 |
| 机制 | v2.0 新增「节拍连续性」给与参考倍频一致的候选 ×1.6 增益；一旦某时刻误判 198，198 候选持续获得 ×1.6（0.452×1.6=0.723 > 0.601），形成**正反馈永久锁死**——即使 9s 后 148 层占据主导（原始证据 148 强 33%）也无法挣脱 |
| 验证 | 逐块更新模拟：7 种策略全部锁定 198（修复前）；整段 60s 峰值表 148:0.601 vs 198:0.452；8s 滑动窗口（8–16s 段）读 148.0 |
| 修复 | 连续性只作 **tie-breaker**：原始得分低于最佳候选 90% 的候选不享受连续性加分（0.452 < 0.9×0.601=0.541 → 增益失效） |
| 结果 | 修复后 7 种策略全部收敛 148（末 30s 54/60 次为 148）；前 ~8s 显示 ~197（开头段落真实存在的 4/3 韵律层），9s 后稳定 148 |
| 回归 | 节拍器 60–200 / 30–400 与 8 首真实音乐范围测试全通过，与 v2.0 数值一致 |

### 上限扩展：BPM 400（v2.2）

- 所有方案「最大 BPM」滑块上限 **300 → 400**（默认仍为 200，避免普通曲目
  被 2 倍速谐波带偏；检测时设置范围如 300–400 即可测 321+）。
- 合成节拍器 321.321 / 340 / 380 / 400 BPM 精度：误差 ≤0.29 BPM，
  置信度 ≥0.95；极端扫描（至 400）最大误差 0.32 不变。

### v2.2 验证：Supa7onyz - DESTRUCTION 3,2,1（321.321 BPM）

| 项目 | 结果 |
|---|---|
| 文件 | `Supa7onyz - DESTRUCTION 3,2,1.flac`（44.1kHz，158s） |
| 整段 60s 批处理（范围 300–400） | **321.63 BPM，conf 0.86**（误差 0.31） |
| 流式（app 节奏，范围 300–400） | 7 种策略全部 321.5–321.8 稳定锁定 |
| 默认范围 60–200 | 160.8（半速）——预期行为，需调范围才能覆盖 321 |
| 自相关峰值表（60s） | lag 32 → 323.0 BPM (0.858) 为最强峰，且短周期优先规则选中它 |
| 合成 321.321 精度 | 321.504，误差 0.183，conf 0.992 |

结论：检测有效，误差 <0.3 BPM；已加入 `test_track_regression.py`（范围 300–400，FAILURES: 0）。

---

## 后续可做方向

- 对更多真实音乐进行批量实听测试
- 增加“自动识别风格并推荐 BPM 范围”
- 增加自动半速/倍速修正 UI
- 增加 BPM 历史曲线图
- 增加节拍锁定视觉指示器
- 增加安装包分发
