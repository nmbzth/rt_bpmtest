# AGENT_2.md — v3.0 三位小数 BPM 精度计划

> 目标：在现有 v2.2（无偏自相关 + 增量引擎，误差 ~0.3 BPM @321）之上，
> 把稳态段落的 BPM 精度压到 **0.001 BPM 量级**，UI 三位小数显示并给出
> ±σ 不确定度；用 git 保存进度；实现后跑完整验证。

---

## 1. 背景与可行性

### 物理限制（321.321 BPM 时）

| 环节 | 现状 | 结论 |
|---|---|---|
| hop=128 网格 | 一格 5.8ms = ±4.9 BPM | 整数 lag 不可能三位小数 |
| 抛物线插值 | 误差 0.18–0.3 BPM | 只用了 3 个 AC 点，峰形不对称 |
| 8s 窗口 | 仅 ~43 拍，统计误差 ≈0.26 BPM | 当前 0.3 级误差主因 |

- BPM 三位小数 = 周期 186.73ms 的 3e-6 相对精度；
- **关键认识**：不是靠"提高自相关分辨率"，而是靠把几百个拍的相位一起拟合；
- 统计可行性：σ_t≈0.1ms、N≈321 拍（60s）时 σ_T≈6e-5 ms →
  σ_BPM≈1e-4，**统计上远超 0.001 需求**；
- 真正的瓶颈是**系统性偏差**（onset 定位的波形模式噪声、拍速漂移、
  采样时钟），因此方案要点是"大量拍整体拟合 + 残差诊断 + σ 标定显示位数"。

---

## 2. 实现方案（最小可行路线）

### 2.1 总体架构

```
粗估计(现有, ±0.5 BPM) ──► 子采样 onset 定位 ──► 加权最小二乘周期回归 ──► 显示 3 位小数 + ±σ
```

细化挂在 `BaseStrategy._autocorr()` 之后，**7 种策略统一受益**，
不改变粗估计选峰逻辑（低风险）。

### 2.2 子采样 onset 定位（σ_t：5.8ms → ~0.1ms 级）

- 粗 onset 由现有包络 + 抛物线得到（hop=128 网格）；
- 每个粗 onset 在 **原始音频**上做局部细化：取 ±2ms 音频，计算局部
  能量（或 hop=16 精细频谱通量）包络，抛物线/样条定位峰值；
  - 局部能量法最简：`e[i]=x[i]²`，5 样本平滑后抛物线顶点；
  - 备选：并行 `OnsetEngine(sr, fft, hop=16)` 精细引擎（增量，预计
    每块额外 ~1–2ms CPU，1.3% 预算可接受）；
  - 先实现局部能量法，若 σ 不达标再上精细引擎/多层细化。

### 2.3 加权最小二乘周期回归（核心，等价于连续 comb 拟合）

- 模型：`t_i = a + k_i·T + d·k_i²`（含线性拍速漂移项，真实曲目必备）；
- 输入：细化后的 onset 时间 t_i（绝对值，秒）、权重 w_i（onset 显著度）、
  拍号 k_i = round((t_i - t0)/T_coarse)；
- 残差过滤：|t_i - a - k_i·T| > 0.15·T 的 onset 丢弃（镲片/幽灵音等
  非网格事件），过滤后重估；
- 求解：加权 3×3 正规方程（numpy 闭式解 + 一次迭代重排拍号）；
- 输出：`T`（周期）、`σ_T`（从协方差矩阵 var(T)=s²·C[T,T]，s²=RSS/(n-3)）；
  `BPM = 60/T`，`σ_BPM = 60·σ_T/T²`。
- 约束：强拍数 n ≥ 6 才启用细化，否则回退粗估计（短窗口/冷启动）。

### 2.4 数据与显示

- `BpmResult` 新增字段：`sigma_bpm: float = 0.0`、`beats: int = 0`；
- UI：主 BPM 三位小数（`321.321`），详情区显示 `±0.0004 (312拍)`；
- 置信度低于阈值或 σ 较大时仍显示三位但以 σ 提示（诚实原则）；
- 版本号 v3.0。

### 2.5 性能

- 细化只在更新时执行：每更新 O(拍数×局部窗) ≈ 数百次小运算，<1ms；
- 与现有 0.2–0.5ms/更新的粗估计叠加后仍远低于 0.5s 更新间隔。

---

## 3. 验证方案

### 3.1 合成精度（判据：|误差| ≤ 0.0015 且 σ ≤ 0.0005）

- 新脚本 `test_precision.py`：非整数 BPM 精准合成
  `321.321 / 317.777 / 320.000 / 148.000 / 180.000 / 65.000`，60s 稳态；
  - 合成信号用精确相位 `i = round(k·60/bpm·sr)`，含真实包络形状；
- 多相位/多时长（15s/30s/60s）扫描：看 σ_BPM 随窗口的收敛曲线。

### 3.2 真实曲目

| 曲目 | 真值 | 判据 |
|---|---|---|
| Supa7onyz - DESTRUCTION 3,2,1 | 321.321 | ≤0.0015（需范围 300–400） |
| 夢世界フォークロア | 148 | ≤0.003（148 半速锁死回归） |
| rock_hiking_180 / electronic_rise_132 | 180 / 132 | ≤0.003（±12 范围） |

- 分段测（30s 切片 × N）：跨段一致性 σ，检验拍速稳定性；
- 若曲目本身存在速度渐变，报告"分段均值 + 段间 σ"，不硬凑整段三位。

### 3.3 回归与性能

- `test_sweep.py` / `test_extreme_sweep.py` / `test_music_ranged.py` /
  `test_track_regression.py` 全通过（容差不变）；
- `bench_compare.py`：确认细化后单次更新仍在 1ms 级；
- 全量跑通后重建 `dist/RTBPM-v3.0.exe`。

---

## 4. Git 保存与交付顺序

1. `git init` + `.gitignore`（.venv / build / dist / __pycache__），
   提交当前 v2.2 完整进度（含回归测试、文档、3 首测试曲目）；
2. 实现细化链路（2.2–2.4）；
3. 跑 3.1 合成验证并调参（阈值/漂移项/局部窗长）；
4. 跑 3.2 真实曲目 + 3.3 全回归；
5. 更新 AGENT.md / TEST_REPORT.md，重建 exe；
6. 提交 v3.0 结果，输出验证报告。

---

## 5. 风险与回退

| 风险 | 对策 |
|---|---|
| 真实曲目拍速漂移导致三位小数无意义 | 显示 ±σ；给出分段均值 |
| 细化反向引入偏差（过拟合局部噪声） | 残差过滤 + n≥6 门槛 + 与粗估计差 >1 BPM 时回退 |
| 性能劣化 | 局部窗限制在 ±2ms；bench_compare 监控 |
| 整体不达标 | 回退到 v2.2 提交点（git tag `v2.2-baseline`） |
